platform :ios, '13.0'

install! 'cocoapods', :disable_input_output_paths => true

inhibit_all_warnings!
use_modular_headers!

target 'Kathari' do
    pod 'SnapKit', '~> 5.0.0'
    pod 'SwiftyUserDefaults'
    pod 'TactileSlider'

end
